apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sure-lab-vinetos-fr
  namespace: envoy-gateway-system
spec:
  secretName: sure-lab-vinetos-fr-tls
  issuerRef: { name: letsencrypt, kind: ClusterIssuer }
  dnsNames: ["sure.lab.vinetos.fr"]