apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: affine-vinetos-fr
  namespace: envoy-gateway-system
spec:
  secretName: affine-vinetos-fr-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - affine.vinetos.fr